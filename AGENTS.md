# Jinkakuの作業を引き継ぐエージェントへ

編集前に [docs/CHAT_HANDOFF.md](docs/CHAT_HANDOFF.md) と、最新の
`.github/workflows/android.yml` を読むこと。

- このリポジトリは、保存されたベースソースへPythonパッチを順番に適用してからAPKをビルドする。保存ソースだけを見て完成版のUIや挙動を判断しない。
- パッチの順番、文字列の置換元、適用済みマーカーには依存関係がある。対象を検索し、実際のワークフローと同じ順序で適用結果を確認する。
- ワードマークの差し替え依頼では、提供された画像をアプリの画像リソースとして組み込む。画像生成へ置き換えない。
- ヘッダー専用画像は `app/src/main/res/drawable-nodpi/jinkaku_header_wordmark.png`。旧 `jinkaku_wordmark_png.png` はアプリアイコンにも使われるため、ヘッダーだけの変更では編集しない。
- 画像の実際の形式・透過・解像度を確認する。拡張子だけを変更したり、受け渡しの都合で極端な縮小・減色をしたりしない。
- APKを更新配布するときは最新のバージョンを調べ、`gradle.properties` の名前とコードを進める。アプリIDと既存の署名設定は上書き更新との互換性に関わる。
- ソースへの反映、CIビルド、APKの公開、実機確認は別々に扱い、実際に確認できた範囲を報告する。
- ここに書かれたv0.1.53時点のパスや手順は、作業時の最新ソース・ワークフローと照合する。
