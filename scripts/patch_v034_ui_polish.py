from pathlib import Path
import re


def replace_once(text: str, old: str, new: str, label: str) -> str:
    if old not in text:
        raise RuntimeError(f"anchor not found: {label}")
    return text.replace(old, new, 1)


def sub_once(text: str, pattern: str, replacement: str, label: str) -> str:
    updated, count = re.subn(pattern, replacement, text, count=1, flags=re.S)
    if count != 1:
        raise RuntimeError(f"regex anchor not found: {label}")
    return updated


def main() -> None:
    path = Path("app/src/main/java/com/ikegami99/jinkaku/ui/ModernJinkakuApp.kt")
    text = path.read_text(encoding="utf-8")

    if "UI_POLISH_V034" in text:
        print("UI_POLISH_V034 already applied")
        return
    if "VISUAL_THEME_V031" not in text or "MEMORY_EDIT_DELETE_V029" not in text:
        raise RuntimeError("v0.1.31 visual theme and v0.1.29 accent patches must run before UI_POLISH_V034")

    text = replace_once(text, "import androidx.compose.foundation.background\n", "import androidx.compose.foundation.Image\nimport androidx.compose.foundation.background\n", "Image import")
    text = replace_once(text, "import androidx.compose.material3.FilterChip\n", "import androidx.compose.material3.FilterChip\nimport androidx.compose.material3.FilterChipDefaults\n", "FilterChipDefaults import")
    text = replace_once(text, "import androidx.compose.material3.IconButton\n", "import androidx.compose.material3.IconButton\nimport androidx.compose.material3.IconButtonDefaults\n", "IconButtonDefaults import")
    text = replace_once(text, "import androidx.compose.ui.graphics.Color\n", "import androidx.compose.ui.graphics.Color\nimport androidx.compose.ui.layout.ContentScale\n", "ContentScale import")
    text = replace_once(text, "import androidx.compose.ui.platform.LocalSoftwareKeyboardController\n", "import androidx.compose.ui.platform.LocalSoftwareKeyboardController\nimport androidx.compose.ui.res.painterResource\n", "painterResource import")
    text = replace_once(text, "import com.ikegami99.jinkaku.JinkakuViewModel\n", "import com.ikegami99.jinkaku.JinkakuViewModel\nimport com.ikegami99.jinkaku.R\n", "R import")
    text = replace_once(text, "import java.util.Locale\n", "import java.util.Locale\nimport org.json.JSONObject\n", "JSONObject import")

    old_title = '''                        title = {\n                            Column {\n                                Text("Jinkaku", fontWeight = FontWeight.Bold)\n                                Row(verticalAlignment = Alignment.CenterVertically, horizontalArrangement = Arrangement.spacedBy(6.dp)) {\n                                    StatusDot(ui.busy)\n                                    Text(\n                                        "$statusText  ·  Memory ${ui.memoryCount}",\n                                        style = MaterialTheme.typography.labelMedium,\n                                        color = MaterialTheme.colorScheme.onSurfaceVariant\n                                    )\n                                }\n                            }\n                        },\n'''
    new_title = '''                        title = {\n                            Column(verticalArrangement = Arrangement.spacedBy(1.dp)) {\n                                JinkakuWordmark(Modifier.width(138.dp).height(22.dp))\n                                Row(verticalAlignment = Alignment.CenterVertically, horizontalArrangement = Arrangement.spacedBy(6.dp)) {\n                                    StatusDot(ui.busy)\n                                    Text(\n                                        "$statusText  ·  Memory ${ui.memoryCount}",\n                                        style = MaterialTheme.typography.labelSmall,\n                                        color = MaterialTheme.colorScheme.onSurfaceVariant.copy(alpha = 0.74f)\n                                    )\n                                }\n                            }\n                        },\n'''
    text = replace_once(text, old_title, new_title, "wordmark top app bar")

    text = replace_once(text, '''                                    contentPadding = PaddingValues(horizontal = 14.dp, vertical = 8.dp),\n                                    modifier = Modifier.padding(end = 6.dp)\n                                ) {\n                                    Icon(Icons.Rounded.Add, null, modifier = Modifier.size(18.dp))\n                                    Spacer(Modifier.width(4.dp))\n                                    Text("New")\n''', '''                                    contentPadding = PaddingValues(horizontal = 10.dp, vertical = 6.dp),\n                                    modifier = Modifier.padding(end = 3.dp)\n                                ) {\n                                    Icon(Icons.Rounded.Add, null, modifier = Modifier.size(16.dp))\n                                    Spacer(Modifier.width(3.dp))\n                                    Text("New", style = MaterialTheme.typography.labelLarge)\n''', "compact New button")

    text = sub_once(text, r'''@Composable\nprivate fun BrandHeader\(modifier: Modifier = Modifier\) \{.*?\n\}\n\n@Composable\nprivate fun StatusDot''', '''@Composable\nprivate fun JinkakuWordmark(modifier: Modifier = Modifier) {\n    Image(\n        painter = painterResource(R.drawable.jinkaku_wordmark),\n        contentDescription = "Jinkaku",\n        contentScale = ContentScale.Fit,\n        modifier = modifier\n    )\n}\n\n@Composable\nprivate fun BrandHeader(modifier: Modifier = Modifier) {\n    Column(modifier, verticalArrangement = Arrangement.spacedBy(5.dp)) {\n        JinkakuWordmark(Modifier.width(170.dp).height(27.dp))\n        Text(\n            "Local persona AI",\n            style = MaterialTheme.typography.labelSmall,\n            color = MaterialTheme.colorScheme.onSurfaceVariant.copy(alpha = 0.70f)\n        )\n    }\n}\n\n@Composable\nprivate fun StatusDot''', "drawer brand header")

    text = sub_once(text, r'''@Composable\nprivate fun ModernContextCard\(telemetry: InferenceTelemetryState, configuredContext: Int, modifier: Modifier = Modifier\) \{.*?\n\}\n\n@Composable\nprivate fun ModernMemoryScreen''', '''@Composable\nprivate fun ModernContextCard(telemetry: InferenceTelemetryState, configuredContext: Int, modifier: Modifier = Modifier) {\n    val max = if (telemetry.contextMax > 0) telemetry.contextMax else configuredContext\n    val used = if (telemetry.contextMax > 0) telemetry.contextUsed.coerceIn(0, max) else 0\n    val remaining = (max - used).coerceAtLeast(0)\n    val fraction = if (max > 0) used.toFloat() / max else 0f\n\n    Card(\n        modifier = modifier.fillMaxWidth(),\n        colors = CardDefaults.cardColors(\n            containerColor = MaterialTheme.colorScheme.surfaceVariant.copy(alpha = 0.34f),\n            contentColor = MaterialTheme.colorScheme.onSurface\n        ),\n        shape = RoundedCornerShape(18.dp)\n    ) {\n        Column(Modifier.padding(horizontal = 14.dp, vertical = 9.dp), verticalArrangement = Arrangement.spacedBy(7.dp)) {\n            Row(Modifier.fillMaxWidth(), verticalAlignment = Alignment.CenterVertically) {\n                Text(\n                    "CONTEXT  $used / $max tok",\n                    style = MaterialTheme.typography.labelMedium,\n                    fontWeight = FontWeight.SemiBold,\n                    color = MaterialTheme.colorScheme.onSurface.copy(alpha = 0.86f)\n                )\n                Spacer(Modifier.weight(1f))\n                Text(\n                    "残り $remaining",\n                    style = MaterialTheme.typography.labelMedium,\n                    color = MaterialTheme.colorScheme.onSurfaceVariant.copy(alpha = 0.78f)\n                )\n            }\n            LinearProgressIndicator(\n                progress = { fraction.coerceIn(0f, 1f) },\n                modifier = Modifier.fillMaxWidth().height(3.dp).clip(RoundedCornerShape(999.dp))\n            )\n        }\n    }\n}\n\n@Composable\nprivate fun ModernMemoryScreen''', "compact context card")

    text = text.replace("modifier = Modifier.fillMaxWidth(0.88f)", "modifier = Modifier.fillMaxWidth(0.84f)")
    text = text.replace("MaterialTheme.colorScheme.surfaceVariant.copy(alpha = 0.7f)", "MaterialTheme.colorScheme.surfaceVariant.copy(alpha = 0.56f)")
    text = replace_once(text, '''                            modifier = Modifier.padding(start = 10.dp, top = 5.dp),\n                            style = MaterialTheme.typography.labelSmall,\n                            color = MaterialTheme.colorScheme.onSurfaceVariant\n''', '''                            modifier = Modifier.padding(start = 8.dp, top = 4.dp),\n                            style = MaterialTheme.typography.labelSmall,\n                            color = MaterialTheme.colorScheme.onSurfaceVariant.copy(alpha = 0.62f)\n''', "quiet inference telemetry")

    text = replace_once(text, '''        Surface(\n            color = MaterialTheme.colorScheme.surface.copy(alpha = 0.72f),\n            contentColor = MaterialTheme.colorScheme.onSurface,\n            tonalElevation = 0.dp\n        ) {\n''', '''        Surface(\n            color = MaterialTheme.colorScheme.surface.copy(alpha = 0.54f),\n            contentColor = MaterialTheme.colorScheme.onSurface,\n            tonalElevation = 0.dp\n        ) {\n''', "lighter composer surface")
    text = replace_once(text, '''                Modifier.fillMaxWidth().imePadding().navigationBarsPadding().padding(horizontal = 12.dp, vertical = 10.dp),\n''', '''                Modifier.fillMaxWidth().imePadding().navigationBarsPadding().padding(horizontal = 12.dp, vertical = 7.dp),\n''', "composer padding")
    text = replace_once(text, '''                    shape = RoundedCornerShape(24.dp),\n                    maxLines = 6\n''', '''                    shape = RoundedCornerShape(22.dp),\n                    maxLines = 5\n''', "composer field shape")
    text = replace_once(text, '''                    enabled = ui.busy || input.isNotBlank(),\n                    modifier = Modifier.size(54.dp)\n                ) {\n''', '''                    enabled = ui.busy || input.isNotBlank(),\n                    colors = IconButtonDefaults.filledIconButtonColors(\n                        containerColor = if (ui.busy || input.isNotBlank()) MaterialTheme.colorScheme.primary else MaterialTheme.colorScheme.surfaceVariant,\n                        contentColor = if (ui.busy || input.isNotBlank()) MaterialTheme.colorScheme.onPrimary else MaterialTheme.colorScheme.onSurfaceVariant\n                    ),\n                    modifier = Modifier.size(48.dp)\n                ) {\n''', "active send button")

    text = replace_once(text, '''private fun modernMemoryDate(timestamp: Long): String = SimpleDateFormat("yyyy/MM/dd HH:mm", Locale.getDefault()).format(Date(timestamp))\n''', '''private fun modernMemoryDate(timestamp: Long): String = SimpleDateFormat("yyyy/MM/dd HH:mm", Locale.getDefault()).format(Date(timestamp))\n\n// UI_POLISH_V034\nprivate data class PersonaOverview(val name: String, val traits: List<Pair<String, Int>>)\n\nprivate fun parsePersonaOverview(raw: String): PersonaOverview? = runCatching {\n    val json = JSONObject(raw)\n    val traits = json.optJSONObject("traits")\n    val mapping = listOf(\n        "curiosity" to "好奇心",\n        "independence" to "独立性",\n        "sarcasm" to "皮肉",\n        "empathy" to "共感性"\n    )\n    PersonaOverview(\n        name = json.optString("name").ifBlank { "名称未設定" },\n        traits = mapping.mapNotNull { (key, label) ->\n            if (traits?.has(key) == true) {\n                label to (traits.optDouble(key, 0.0) * 100.0).toInt().coerceIn(0, 100)\n            } else null\n        }\n    )\n}.getOrNull()\n''', "persona overview helper")
    text = replace_once(text, '''    var personaDraft by remember { mutableStateOf(vm.currentPersona()) }\n''', '''    var personaDraft by remember { mutableStateOf(vm.currentPersona()) }\n    var personaAdvanced by remember { mutableStateOf(false) }\n    val personaOverview = remember(personaDraft) { parsePersonaOverview(personaDraft) }\n''', "persona view state")

    text = sub_once(text, r'''        item \{ ModernSectionTitle\("外観", Icons\.Rounded\.DarkMode\) \}.*?        item \{ ModernSectionTitle\("Persona", Icons\.Rounded\.Psychology\) \}''', '''        item { ModernSectionTitle("外観", Icons.Rounded.DarkMode) }\n        item {\n            ModernSettingsCard {\n                Column(verticalArrangement = Arrangement.spacedBy(16.dp)) {\n                    Row(Modifier.fillMaxWidth(), verticalAlignment = Alignment.CenterVertically) {\n                        Surface(shape = RoundedCornerShape(14.dp), color = MaterialTheme.colorScheme.primaryContainer, modifier = Modifier.size(42.dp)) {\n                            Box(contentAlignment = Alignment.Center) { Icon(if (darkMode) Icons.Rounded.DarkMode else Icons.Rounded.LightMode, null) }\n                        }\n                        Spacer(Modifier.width(12.dp))\n                        Column(Modifier.weight(1f)) {\n                            Text("ダークモード", fontWeight = FontWeight.SemiBold)\n                            Text("アプリ全体の配色を切り替えます", style = MaterialTheme.typography.bodySmall, color = MaterialTheme.colorScheme.onSurfaceVariant)\n                        }\n                        Switch(checked = darkMode, onCheckedChange = onDarkModeChange)\n                    }\n\n                    HorizontalDivider(color = MaterialTheme.colorScheme.outline.copy(alpha = 0.22f))\n\n                    Column(verticalArrangement = Arrangement.spacedBy(8.dp)) {\n                        Text("アクセントカラー", fontWeight = FontWeight.SemiBold)\n                        Text("会話やボタンの強調色。選んだ色をチップ全体にも薄く反映します。", style = MaterialTheme.typography.bodySmall, color = MaterialTheme.colorScheme.onSurfaceVariant)\n                        ModernAccents.chunked(3).forEach { row ->\n                            Row(Modifier.fillMaxWidth(), horizontalArrangement = Arrangement.spacedBy(6.dp)) {\n                                row.forEach { accent ->\n                                    val swatch = if (darkMode) accent.darkPrimary else accent.lightPrimary\n                                    FilterChip(\n                                        selected = accentColor == accent.name,\n                                        onClick = { onAccentColorChange(accent.name) },\n                                        label = { Text(accent.name) },\n                                        leadingIcon = { Surface(shape = RoundedCornerShape(999.dp), color = swatch, modifier = Modifier.size(14.dp)) {} },\n                                        colors = FilterChipDefaults.filterChipColors(\n                                            containerColor = swatch.copy(alpha = if (darkMode) 0.08f else 0.06f),\n                                            selectedContainerColor = swatch.copy(alpha = if (darkMode) 0.28f else 0.20f)\n                                        ),\n                                        modifier = Modifier.weight(1f)\n                                    )\n                                }\n                            }\n                        }\n                    }\n                }\n            }\n        }\n\n        item { ModernSectionTitle("Persona", Icons.Rounded.Psychology) }''', "merged appearance and accent settings")

    text = sub_once(text, r'''        item \{ ModernSectionTitle\("Persona", Icons\.Rounded\.Psychology\) \}\n        item \{.*?\n        item \{ ModernSectionTitle\("モデル", Icons\.Rounded\.Storage\) \}''', '''        item { ModernSectionTitle("Persona", Icons.Rounded.Psychology) }\n        item {\n            ModernSettingsCard {\n                Column(verticalArrangement = Arrangement.spacedBy(12.dp)) {\n                    val overview = personaOverview\n                    if (overview != null) {\n                        Text(overview.name, style = MaterialTheme.typography.titleMedium, fontWeight = FontWeight.Bold)\n                        if (overview.traits.isNotEmpty()) {\n                            overview.traits.chunked(2).forEach { row ->\n                                Row(Modifier.fillMaxWidth(), horizontalArrangement = Arrangement.spacedBy(8.dp)) {\n                                    row.forEach { (label, value) ->\n                                        Surface(shape = RoundedCornerShape(14.dp), color = MaterialTheme.colorScheme.primary.copy(alpha = 0.10f), modifier = Modifier.weight(1f)) {\n                                            Column(Modifier.padding(horizontal = 12.dp, vertical = 9.dp)) {\n                                                Text(label, style = MaterialTheme.typography.labelSmall, color = MaterialTheme.colorScheme.onSurfaceVariant)\n                                                Text("$value / 100", fontWeight = FontWeight.SemiBold)\n                                            }\n                                        }\n                                    }\n                                    if (row.size == 1) Spacer(Modifier.weight(1f))\n                                }\n                            }\n                        }\n                    } else {\n                        Text("PersonaをJSONとして解析できません。詳細編集から内容を確認できます。", color = MaterialTheme.colorScheme.onSurfaceVariant)\n                    }\n\n                    TextButton(onClick = { personaAdvanced = !personaAdvanced }) { Text(if (personaAdvanced) "詳細編集を閉じる" else "詳細編集（JSON）") }\n\n                    if (personaAdvanced) {\n                        OutlinedTextField(\n                            value = personaDraft,\n                            onValueChange = { personaDraft = it },\n                            modifier = Modifier.fillMaxWidth().heightIn(min = 150.dp),\n                            label = { Text("Jinkakuの基本人格") },\n                            minLines = 5,\n                            maxLines = 12,\n                            shape = RoundedCornerShape(18.dp)\n                        )\n                    }\n\n                    Row(horizontalArrangement = Arrangement.spacedBy(8.dp)) {\n                        if (personaAdvanced) {\n                            Button(onClick = { vm.savePersona(personaDraft) }, enabled = personaDraft.isNotBlank() && !ui.busy) {\n                                Icon(Icons.Rounded.Edit, null, modifier = Modifier.size(18.dp)); Spacer(Modifier.width(5.dp)); Text("保存")\n                            }\n                        }\n                        OutlinedButton(onClick = { personaDraft = vm.currentPersona() }) { Text("再読込") }\n                    }\n                    Text("Personaの一部を長期Memoryにも残したい場合はMemory画面からPERSONAとして追加できます。", style = MaterialTheme.typography.bodySmall, color = MaterialTheme.colorScheme.onSurfaceVariant.copy(alpha = 0.74f))\n                }\n            }\n        }\n\n        item { ModernSectionTitle("モデル", Icons.Rounded.Storage) }''', "friendly Persona settings")

    text = text.replace("Color(0xFF20D6C7).copy(alpha = if (darkMode) 0.24f else 0.20f)", "Color(0xFF20D6C7).copy(alpha = if (darkMode) 0.22f else 0.18f)")
    text = text.replace("center = Offset(1040f, 650f)", "center = Offset(1040f, 300f)")
    old_final_aurora = '''                    .background(\n                        Brush.radialGradient(\n                            colors = listOf(\n                                Color(0xFFFF5FA2).copy(alpha = if (darkMode) 0.16f else 0.14f),\n                                Color.Transparent\n                            ),\n                            center = Offset(980f, 1850f),\n                            radius = 1350f\n                        )\n                    ),\n'''
    new_final_aurora = '''                    .background(\n                        Brush.radialGradient(\n                            colors = listOf(\n                                Color(0xFFFF5FA2).copy(alpha = if (darkMode) 0.15f else 0.13f),\n                                Color.Transparent\n                            ),\n                            center = Offset(980f, 1900f),\n                            radius = 1350f\n                        )\n                    )\n                    .background(\n                        Brush.verticalGradient(\n                            colors = if (darkMode) {\n                                listOf(Color.Transparent, Color.Black.copy(alpha = 0.12f), Color.Transparent)\n                            } else {\n                                listOf(Color.Transparent, Color.Transparent)\n                            },\n                            startY = 420f,\n                            endY = 1700f\n                        )\n                    ),\n'''
    text = replace_once(text, old_final_aurora, new_final_aurora, "localized aurora vignette")

    path.write_text(text, encoding="utf-8")
    print("Applied UI_POLISH_V034: wordmark, quieter hierarchy, compact context, tinted appearance controls and friendly Persona")


if __name__ == "__main__":
    main()
